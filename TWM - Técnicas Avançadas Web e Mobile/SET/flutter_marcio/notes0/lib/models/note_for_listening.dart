class NoteForListening {
  String noteID;
  String noteTitle;
  DateTime createDateTime;
  DateTime lastEditDateTime;

  NoteForListening(this.noteID, this.noteTitle, this.createDateTime, this.lastEditDateTime);

}